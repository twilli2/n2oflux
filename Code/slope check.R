df <- x %>%
  group_by(field, plot, chamber) %>%
  filter(field == 1, plot == "GN0", chamber ==1) 
#do(mod = lm(co2~time,data = .)) %>%
#summarise(co2_rsq = summary(mod)$r.squared)
df
for (i in 1:length(x) {
  print(x[i])
}

for (i in 1:length(df$co2)) {
  print(df$co2[i])
}
sapply(mod, take 1:4, lm(x) 
       1,2,3
       2,3,4
       1,2,4
       
       print(slope) of max(rsq)function)

return_left <- function (x){
  max [,2]
  print[,4]
}
sapply(df,return_left)


df <- data.frame(x = seq(1,10, by = 0.5), y = seq(10,1, by  = -0.5))
df                                                
sapply(x = df$x, y = df$y, 
       
fun_1 <- function (x,y){
  if x[x>y] <- "yes"
  }else x{
  }

df <- 5
if (df == 5) {
print ("Yay")
}

y$co2_slope[y$co2_rsq<=0.45]
y %>% 
  group_by(field, plot, chamber) %>% 
  pmax(co2_rsq, co2_rsq_2)

View(y[12,])#single row
View(y[12])#single column
y %>% 
sapply(pmax)
(field, plot, chamber) %>% 
sapply(pmax)
